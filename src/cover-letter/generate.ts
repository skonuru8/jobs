/**
 * generate.ts — backward-compatible re-exports (legacy imports).
 */

export { generateCoverLetter } from "./generator";
export { generateAndSaveCoverLetter } from "./saver";
export { escapeLatexBody, escapeLatexPlain } from "./saver";
